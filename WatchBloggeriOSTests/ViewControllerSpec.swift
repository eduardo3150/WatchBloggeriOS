import Foundation
import Quick
import Nimble
import Spry_Nimble

@testable import WatchBloggeriOS

class ViewControllerSpec: QuickSpec {
    override func spec() {
        describe("test") {
            it("runs_test") {
                expect(true).to(beTrue())
            }
        }
    }
}
